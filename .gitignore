.venv/
__pycache__/
*.pyc
.env
data/
*.db
*.db-wal
*.db-shm
*.key
.DS_Store
.idea/
.vscode/
.pytest_cache/

# Боевые списки устройств. devices.csv в репозитории — шаблон с выдуманными
# данными; всё, что названо иначе, считается настоящим и не публикуется.
devices-*.csv
*.real.csv
data.bak-*/

# Архивы панели: внутри база и ключ шифрования, то есть пароли всех роутеров
tikpilot-panel-*.tar.gz

# Личная памятка по публикации — в репозитории не нужна
ПУБЛИКАЦИЯ.md
