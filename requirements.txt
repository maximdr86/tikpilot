# Версии указаны «не ниже», а не жёстко: так pip берёт готовые сборки под любую
# версию Python (включая свежие 3.13/3.14) и ничего не компилирует локально.
# Верхние границы отсекают мажорные обновления с ломающими изменениями.

fastapi>=0.115,<1.0
uvicorn[standard]>=0.34,<1.0
jinja2>=3.1,<4.0
python-multipart>=0.0.20
itsdangerous>=2.2,<3.0
cryptography>=44.0
bcrypt>=4.2,<6.0
librouteros>=3.4,<4.0
python-dotenv>=1.0,<2.0
# QR-код конфигурации WireGuard. Чистый Python без своих зависимостей,
# нужен только чтобы не тянуть библиотеку QR из интернета в браузер.
segno>=1.6,<2.0
# Терминал: SSH до роутера. Нужен только странице «Терминал»,
# всё остальное панель делает по API.
paramiko>=3.4,<6.0
